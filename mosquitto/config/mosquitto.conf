# Standard MQTT listener
listener 1883 0.0.0.0
protocol mqtt

# WebSocket listener (Crucial for the React dashboard later)
listener 9001 0.0.0.0
protocol websockets

# For local development, allow anonymous connections without passwords
allow_anonymous true

# Persistence
persistence true
persistence_location /mosquitto/data/